# -*- mode: snippet -*-
#name: flatspec
# --
import org.scalatest._
import org.scalatest.Matchers._

class `(file-name-sans-extension (buffer-name))` extends FlatSpec {
  "`(file-name-sans-extension (buffer-name))`" should "$1" in {
    $0
  }
}