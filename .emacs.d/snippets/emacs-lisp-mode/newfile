# -*- mode: snippet -*-
#name: newfile
# --
;;; `(buffer-name)` --- ${1:Summary} -*- lexical-binding: t -*-

;; Copyright (C) `(format-time-string "%Y")` `copyright-owner`
;; License: `license-url`

;;; Commentary:
;;
;;  ${2:Musings}
;;
;;; Code:

$0

(provide '`(buffer-name)`)

;; Local Variables:
;; compile-command: "cask exec ert-runner `(buffer-name)`"
;; End:

;;; `(buffer-name)` ends here
