#!/bin/bash

export LC_ALL=C
export LANG=C
cd ${HOME}/kerbal

zenity --warning --text "Don't forget to wiggle the joystick and set the throttle to zero or it won't work."
jstest-gtk

exec ./KSP.x86_64
